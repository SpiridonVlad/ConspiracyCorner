spring:
  application:
    name: conspiracy-forum
  
  datasource:
    url: jdbc:postgresql://localhost:5432/conspiracy_forum
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  
  graphql:
    graphiql:
      enabled: true
    path: /graphql
    schema:
      printer:
        enabled: true

server:
  port: 8080

# JWT Configuration
jwt:
  secret: ${JWT_SECRET:Y29uc3BpcmFjeS1mb3J1bS1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tc2lnbmluZy0yMDI0}
  expiration: 86400000

# Forum Configuration
forum:
  secret-code: ${FORUM_SECRET_CODE:TINFOIL2024}

logging:
  level:
    com.conspiracy.forum: INFO
    org.springframework.graphql: DEBUG
