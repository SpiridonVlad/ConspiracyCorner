spring:
  application:
    name: conspiracy-forum-test
  
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.H2Dialect
  
  graphql:
    graphiql:
      enabled: true
    path: /graphql
  
  mail:
    host: localhost
    port: 25

server:
  port: 8080

# JWT Configuration
jwt:
  secret: dGVzdC1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tc2lnbmluZy1pbi10ZXN0cw==
  expiration: 86400000

# Forum Configuration
forum:
  secret-code: TESTCODE

logging:
  level:
    com.conspiracy.forum: WARN
    org.springframework.graphql: WARN
